#include "socket.hpp"
#include <future>

void Socket::private_chat(std::string id, std::string friend_id){//页面


    // if (this->is_connected) {
    //     std::cout << "Private chat with " << friend_id << std::endl;
    //     std::string message;
    //     while (true) {
    //         std::getline(std::cin, message);
    //         if (message == "/exit") {
    //             break;
    //         }
    //         this->send_message(id, friend_id, message);
    //     }
    // }
    // else {
    //     std::cout << "You are not connected to the server" << std::endl;
    // }



}